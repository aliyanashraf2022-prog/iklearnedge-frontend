================================================================================
SUPABASE SQL EDITOR - STEPS
================================================================================

STEP 1: Open Supabase
---------------------
1. Go to: https://supabase.com
2. Login to your account
3. Click on your project: "gloqveyprtflmulzjwau"

STEP 2: Open SQL Editor
-----------------------
1. In the left sidebar, click "SQL Editor" (icon looks like a database)
2. It may take a few seconds to load

STEP 3: Run the SQL
-------------------
Copy the entire SQL code below and paste into the SQL Editor, then click "Run"

================================================================================
SQL CODE - COPY FROM NEXT LINE:
================================================================================

-- Create site_settings table
CREATE TABLE IF NOT EXISTS site_settings (
  id SERIAL PRIMARY KEY,
  setting_key VARCHAR(100) UNIQUE NOT NULL,
  setting_value TEXT,
  setting_type VARCHAR(20) DEFAULT 'string',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Insert default settings
INSERT INTO site_settings (setting_key, setting_value, setting_type) VALUES
  ('primary_color', '#f5a623', 'color'),
  ('secondary_color', '#4a4a4a', 'color'),
  ('accent_color', '#3498db', 'color'),
  ('currency', 'AED', 'string'),
  ('currency_symbol', 'د.إ', 'string'),
  ('site_name', 'IkLearnEdge', 'string')
ON CONFLICT (setting_key) DO NOTHING;

-- Create top_verified_teachers table
CREATE TABLE IF NOT EXISTS top_verified_teachers (
  id SERIAL PRIMARY KEY,
  teacher_id INTEGER NOT NULL,
  position INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(teacher_id)
);

CREATE INDEX IF NOT EXISTS idx_top_verified_teachers_position ON top_verified_teachers(position);

================================================================================
END OF SQL CODE
================================================================================

STEP 4: Verify
--------------
1. Look for "Success" message in the output
2. Optionally click "Tables" in left sidebar to verify tables were created
3. You should see: site_settings and top_verified_teachers tables

STEP 5: Test Your Dashboard
---------------------------
1. Go to: https://iklearnedge.vercel.app
2. Login and try accessing Teacher Dashboard, Admin Dashboard, or Student Dashboard
3. It should now work!

================================================================================