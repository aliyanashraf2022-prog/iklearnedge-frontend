{
  "name": "iklearnedge",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build && node scripts/sync-app-dist.mjs",
    "preview": "npm --prefix app run preview",
    "start": "npm --prefix backend run start"
  }
}
