{
  "name": "iklearnedge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd app && npm install",
    "build": "cd app && npm run build",
    "start": "cd app && npm run preview"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
